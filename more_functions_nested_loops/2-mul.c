#include <stdio.h>
#include "main.h"
/**
* mul - multiplies two integers
* @a: first character of the multiplication
* @b: second character of the multiplication
* Return: always 0
*/

int mul(int a, int b)
{
	return (a * b)
}
